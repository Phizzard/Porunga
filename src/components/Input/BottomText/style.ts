import styled from '@emotion/styled'

export const StyledBottomText = styled.span`
  color: #999999;
  font-size: 0.9rem;
  line-height: 1.5;
  margin-top: 0.125rem;
  box-sizing: border-box;
  font-style: italic;
`
